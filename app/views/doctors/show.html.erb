<h1><%=@doctor.name %></h1>

<h2>Appointments:</h2>
<% @doctor.appointments.each do |app| %>
    <p><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - 
    <%= link_to app.patient.name, patient_path(app.patient) %></p>
<% end %>